<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="loginRecord">
	<insert id="save">
		insert into log(
			emp_no, log_login, log_ip, log_browser
		)
		values(
			#{empNo}, #{logLogin}, #{logIp}, 'N'
		)
	</insert>
</mapper>