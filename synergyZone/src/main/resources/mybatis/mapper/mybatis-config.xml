<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd"> <!-- MyBatis 사이트 참고 -->
<!-- 접속 정보(driverClassName, url, username, password) 쓰지 않아도 됨(root-context.xml에 썼기 때문) -->
<configuration>
	<settings>
		<setting name="jdbcTypeForNull" value="NULL"/> <!-- 만약에 null 데이터가 전달되었으면 빈칸이 아닌 NULL로 인식하도록 설정 -->
	</settings>
    <typeAliases>
        <typeAlias type="org.kh.shareware.calendar.domain.CalSch" alias="CalendarScheduleDto"/>
		<typeAlias type="org.kh.shareware.calendar.domain.Calendar"	alias="CalendarDto"/>
		<typeAlias type="org.kh.shareware.calendar.domain.CalBmk" alias="CalendalBookMarkDto"/>
		<typeAlias type="org.kh.shareware.calendar.domain.CalMem" alias="CalendarMember"/>
    </typeAliases>

    <!-- 매퍼 파일 설정 -->
    <mappers>
        <mapper resource="mappers/calendar-mapper.xml"/>
    </mappers>
</configuration>